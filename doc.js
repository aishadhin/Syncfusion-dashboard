// to know: 
/*
*what is craco?
*what is createContext, useContext and how they work.
*onClick={() => setActiveMenu((prevActiveMenu) => !prevActiveMenu)}
*/ 


// npm install --legacy-peer-deps to install only the right dependency for pasted package.json dependencies

